# WAP to find out factorial of given number using recursion.
def fact(n):
    result = 1
    if n == 0:
        return result
    else:
        result = n * fact(n-1)
        return(result)
print(fact(6))
